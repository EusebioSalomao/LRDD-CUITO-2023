<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>PDF-Teste</title>
    <style>
        body{
            margin: 1rem;
        }
        .text-center{
            text-align: center;
        }
        .row{
            display: inline;
        }
    </style>
</head>
<body>
    <div class="logo text-center mt-3"><img src="./public/img/logor.png" alt=""></div>
    <div class="text-center">
        <p class="m-1">República de Angola</p>
        <p class="m-1">Governo da Província do Bié</p>
        <p class="m-1">Administração Municipal de Catabola</p>
        <p class="fw-bold" >LICEU FELIZBERTO DOMINGO NGUNGA</p>

        <div class="mt-3 lead">PAUTA DO Iº TRIMESTRE</div>
    </div>
    <div class="row"><div class="col-6" > <%= classePDF %> </div> <div class="col-5 text-end" > <span class="fw-bold" >Período:</span> <span> <%= periodoPDF %> </span> </div> </div>
    <div class=""> <span class="fw-bold">Turma: </span> <span> <%= turmaPDF %> </span></div>
    <table class="table table-bordered table-sm">
        <thead class="table-secondary">
            <tr>
                <td>N/O</td>
                <td>Nome Completo</td>
                <% for(let i = 0; i < disciplinas.length; i++) { %>
                    <td> <%= disciplinas[i] %></td>
                  <% } %>
            </tr>
        </thead>
        <tbody>
            <ol>

                <% for(let i = 0; i < pautaT1.length; i++) { %>
                    <tr>
                    <td><li></li></td>
                    <td> <%= pautaT1[i].nome %></td>

                    <% for(let x = 0; x < pautaT1[i].notas.notas.length; x++) { %>
                        <td> <%= pautaT1[i].notas.notas[x].nota %></td>
                      <% } %>

                    </tr>
                  <% } %>
                
            </ol>
        </tbody>
    </table>
</body>
</html>